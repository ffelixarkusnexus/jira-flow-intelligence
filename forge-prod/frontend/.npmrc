# Visx 3.x peer-deps say React 16/17/18 only. Visx is primitive geometry —
# no new-React-API usage — so it works fine on React 19. Skip the strict
# check until Visx 4.x ships stable with React 19 in its peers.
legacy-peer-deps=true
