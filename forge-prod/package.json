{
  "name": "flow-intelligence-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Flow Intelligence — Forge app (resolvers + Custom UI bundle)",
  "scripts": {
    "build:ui": "vite build --config frontend/vite.config.ts",
    "dev:ui": "vite --config frontend/vite.config.ts",
    "tsc": "tsc -p tsconfig.json",
    "lint:forge": "forge lint"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/events": "^2.1.7",
    "@forge/resolver": "^1.6.0"
  },
  "devDependencies": {
    "@forge/cli": "^12.22.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}
